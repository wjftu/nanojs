---
sidebar_position: 9
---

# Key Generator


Generate a random key of a desired key length.



import KeyGenerator from '@site/src/components/KeyGenerator';



### Random Key Generator Tool

<KeyGenerator />


### How to use?

Inputs key length in bytes (default: 32 bytes, the most widely used length is 16 and 32 bytes). Click the button to generate a random key of the input key length.

Displays both:

- Base64 format
- Hex format (often used in low-level crypto tools).

Copy button with inline "✅ Copied!" confirmation message.

